# Max Kirst personal website

Static HTML, CSS and JavaScript website for Max Kirst.

## Local preview

Serve the project directory with any static web server, for example:

```sh
python3 -m http.server 8000
```

Then open http://localhost:8000.

## Deployment

The project has no build step. Vercel can deploy the repository root directly.
